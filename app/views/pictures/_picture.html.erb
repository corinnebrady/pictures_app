
<div class="row">
<!--   <div class="col-sm-6 col-md-4"> -->
    <div class="thumbnail">
      <%= link_to image_tag(picture.picture), picture_path(picture), class: "thumbnail" %>
      <div class="caption">
        <h4><%= picture.name %></h4>
        <h6><%= link_to picture.user.username, user_path(picture.user) %> </h6>
      </div>
    </div>
<!--   </div>
 --></div>


<% if picture.user_id == @authenticated.id %>
  <%= link_to 'Edit Picture', edit_picture_path(picture) %>
  <%= button_to 'Delete Picture', picture_path(picture), :method => 'delete' %>
<% end %>