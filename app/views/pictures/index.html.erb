<h1>This is the page that displays ALL pictures</h1>


  <% @pictures.each do |picture| %>
     <h4><%= picture.name %> </h4>
    <%= link_to (image_tag picture.picture), picture_path(picture) %>
    <p><%= picture.description %> </p>
  <% end %>
